import math
n=int(input("enter the number "))
m=int(input("enter the power"))
print(math.pow(n,m))
#similar to above program as that of squareroot everything is same but here we need to declare 2 variables
#this program can also be done using normal code as using math symbol '**'
